import { style } from "@vanilla-extract/css";

export const homeSectionHeaderStyles = {
  description: style({
    marginTop: 8,
  }),
};

export const homeSectionBodyStyles = {
  self: style({
    marginTop: 20,
    padding: 40,
  }),
};
